{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings-ui",
  "description": "Enable settings window IPC and window controls.",
  "windows": ["settings"],
  "permissions": [
    "core:default",
    "log:default",
    "sql:default",
    "settings-ui-commands"
  ]
}
